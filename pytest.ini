[pytest]
testpaths = ml/tests
pythonpath = .
addopts = -q
markers =
    integration: tests that require trained artifacts (run ml/train_all.py first)
filterwarnings =
    ignore::DeprecationWarning:shap
